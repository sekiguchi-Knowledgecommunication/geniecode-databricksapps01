command: ["python", "app.py"]

resources:
  sql_warehouses:
    - name: default
      warehouse_type: PRO
      cluster_size: MEDIUM

env:
  - name: DATABRICKS_WAREHOUSE_ID
    valueFrom: $.resources.sql_warehouses.default.id
